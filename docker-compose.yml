services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-tilapou}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-tilapou}
      POSTGRES_DB: ${POSTGRES_DB:-tilapou}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-tilapou} -d ${POSTGRES_DB:-tilapou}"]
      interval: 5s
      timeout: 3s
      retries: 10

  api:
    build: .
    environment:
      ADDR: ${ADDR:-:8080}
      DATABASE_URL: ${DATABASE_URL:-postgres://tilapou:tilapou@postgres:5432/tilapou?sslmode=disable}
      LOG_LEVEL: ${LOG_LEVEL:-info}
    ports:
      - "${API_PORT:-8080}:8080"
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "/tilapou", "health"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 5s

volumes:
  pgdata:
